#pragma once

#include<vector>
#include<algorithm>
#include<set>

#include "common.cpp"

using std::vector;
using std::sort;
using std::pair;
using std::set;

string greedy(const vector<string>& S, bool adapted = false);
